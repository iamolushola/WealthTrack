/** Generates a UUID v7 (time-sortable, CHAR(36), no external dependencies). */
export declare function createId(): string;
export declare function nowIso(date?: Date): string;
export declare function sha256(value: string): string;
